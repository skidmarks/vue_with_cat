

// DOM 초기화
document.addEventListener('DOMContentLoaded', init);
function init() {

    // vue application
    // Vue 인스턴스 생성하기
    // 주로 app , or vm(viewmodel)이라는 변수명을 사용한다.
    var app  = new Vue({
        el: '#app',
    });
    
} // END init()